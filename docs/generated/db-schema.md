# Database Schema

No database. AI Voice Studio uses:
- In-memory job queue (`job_manager.py`)
- Local filesystem storage (`backend/uploads/`)
- JSON metadata files (`backend/data/voices_meta.json`, `clips_meta.json`)
